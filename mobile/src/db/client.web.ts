export const getSqlite = () => null;
export const getDb = () => null;

export const initDatabase = async () => null;

export const clearLocalDatabase = async () => {
  // Web is not a supported client runtime.
};
